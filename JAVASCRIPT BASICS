################JAVASCRIPT#####################
PRINT: console.log("komal");  //comment /**/
<script src , async(open script and i will use )/defer(script will be use when all the building is done)
#declare:var n can be redeclared,let n,const
#values:null,symbol(unique and immutable),bigint(n)
#datatype:object(dict),array
#function ;function a()
===:type and value ==value []===[]false(reference are compared) '10'<'9'true  Nan===Nanfalse (undefined converted to NaN)
infinity>100 true string+num=string bool+num=num s>n>b
#in :exist in var or not bool
instanceof: whether instance of data typr or not.+=concatenation
?.:obj.address?.city for substring(same type within type)
if else same ##switch (komal){ case "jn":}
global variable:without let or outofblock
console.error,warn,info,table,time,timeEnd,group,count samething called,trace,
##############################this  and function binding ####################################
#creating new:we can lock one function parameter and store in other function this can be used only in the object itself 
when we save in other function it loses there.
#########BIND############# let function=attribute.bind(obj) instead of the obj.attr
##########(marzi ka this)call when we create new function new(this ki jaga object dedo,arg...)
############apply same but pass argument ike array apply(this,[dh,gcb])
this can be acceseed only in object
###########this always remeber its parent i the arrow fucntion
const person = {
    name: 'GFG',
    greet function(){
        const arrowGreet = () => {
            console.log('Hello, ' + this.name); 
        };
        arrowGreet();
    }
};
person.greet();
#########################hoisting########################
hoisting:declaration upper in code 
only happens with the function and var(only declare not initialize) all are hosted but cannot be used tdz(temporary dead zone)
######################closure#######################
when we return the inner fucntino it will remember the outer variables this is closure 
##actually the inner function remembers the variable but not this(this is run time thing)var is in memory 
#if we want this to remenber then we use arrow fucntion
##################scope#####################
{}:scope of that specific thing
let and let are {} oriented not var which has global scope 
##############################IIFE(immediately invoked function expression)###############
()();
function:execution (to prevent global scope declaration)
(function(para){})(argument); due to this the variables within this remain private 
################################setTimeout and clearTimeout###########################
run once after this time
setTimeout(function,millisecond,arg.....)
clearTimeout(var) stop the function before the time 
##################getters,setters,const#####################
we made the attributes inside the constructor
get and set keywords are used consider as attribute 
we use use to create the instance 
dt var=new obj(parameter)
setTimeout in obj we use bind with this 
##############################HIGHER ORDER FUNCTION OF ARRAY#######################
function that takes the function as argument::
passing a function normal leeter calling letter()
array.map(function)   map this fucntion to all elements of the array
array.filter(function(return only true or false)) returns the elements where the fucntion returns true
array.reduce(fucntion, initialValue of acc);
reduce function:function(accumulator, currentValue) {}
array.forEach(funtion) not create new array
array.find(function) return first element at which cond is true 1 element
array.some(funtion) return true or false whether any element satisfy the condition
array.every()    return true or false whehter every elemnt satisfy or not 
#########function composition:
function compose(f, g) {
    return function(x) {
        return f(g(x));
  };
##########function currying:
divide the parameters into single parameter with multiple function
function mul(x) {
    return function(y) {
        return x * y;
  };
}
#############link with the html############
let var=document.getElementById or classname or tagname or querySelector or querySelectorAll("")
              
########methods of the js#####
=>alert()   on browsre pop up sms
=>.addEventHandler(event=click,mouseover,keydown,submit,load,function(),usecapture)
usecapture:true(first the window will know)
flase(first the element will know)
=>.removeEventHandler(event=click,mouseover,keydown,submit,load,function(e))
*******************e***************8
event.key        // The actual character pressed ("a", "Enter", "ArrowUp")
event.code       // Physical key location ("KeyA", "Enter", "ArrowUp")
event.which      // Deprecated - use .key or .code instead
event.ctrlKey    // Boolean - was Ctrl pressed?
event.shiftKey   // Boolean - was Shift pressed?
event.altKey     // Boolean - was Alt pressed?
event.metaKey    // Boolean - was Command (Mac) or Windows key pressed?
event.repeat     // Boolean - is this from holding down a key?
event.target     // The element that triggered the event (inputField)
event.currentTarget // The element the listener is attached to
event.type       // The event type ("keyup", "click", etc.)
event.timeStamp  // When the event occurred (milliseconds)
event.bubbles    // Boolean - does this event bubble up?
=>event.preventDefault()browser default action
=>event.stopPropagation() event flow
###########################DOM(tree structure)#############
textchange*******element.textContent or innerHTML(change the text content) or
attribute*****element.src etcccc
create*********element.createElement and then append this by document.body.appendChild(element)
remove**********remove()
###########javascript works with the BOM and DOM
**********transversing:
=>parentNode
=>children
=>firstChild / lastChild
=>nextSibling / previousSibling
##############class add#########
element.classList.add()
element.classList.remove() or toggle()
list.insertBefore(newItem, list.children[1]);
################ATTRIBUTES#############
getAttribute(): Retrieves the value of an attribute.
setAttribute(): Sets a new value for an attribute.
removeAttribute(): Removes an attribute
dataset.att (adding new attribute)
classback=In JavaScript, a callback is just a function you give to another function, saying:

“Hey, when you’re done, run this for me.” and function can be given changing
#########################
promise##
Promise: You get a “magic box” now and can check later whether it gave you cookies or not.

###########################REGULAR EXPRESSION########################
new RegExp(pattern,flag) class
#####METHODS################
f you want true/false → use .test()
If you want match details → use .exec() or .match()
If you want positions → use .search()
If you want to change text → use .replace() or .replaceAll()
If you want to break text apart → use .split()


between slashes /like this/
\d → any digit (0-9)
\w → any letter or number
. → any single character
* → repeat 0 or more times (bhle na bhi ho) 
+ → repeat 1 or more times (aik dafa to ho)
?-> 0 or 1 
^ → start of text between if 
$ → end of text
m{X, Y}	Find the match of any string that contains a sequence of m, X to Y times
if not want to make the specific function use the \ before that
#####flag after the / of regex
Flag	Name	What it does
g	Global	Finds all matches, not just the first one.
i	Ignore case	Matches A and a the same way.
m	Multiline	Makes ^ and $ match the start/end of each line, not just the whole string.
s	Dotall	Makes . match newline characters too. Without s, . stops at a line break.
u	Unicode	Treats the pattern as Unicode, so you can match emoji, special characters, etc. correctly.
y	Sticky	Matches from the exact position you left off last time (no skipping ahead).

